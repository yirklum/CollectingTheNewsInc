    function showPinkUniform(imgs) {
        $("#pink-uniform").hide();

        var expandPinkUniform = document.getElementById("expandedPinkUniform");

        expandPinkUniform.src = imgs.src;

        expandPinkUniform.parentElement.style.display = "block";
    }

    function showArnettBook(imgs) {
        $("#arnett-book").hide();

        var expandArnettBook = document.getElementById("expandedArnettBook");

        expandArnettBook.src = imgs.src;

        expandArnettBook.parentElement.style.display = "block";
    }

    function showBabyMilk(imgs) {
        $("#baby-milk").hide();

        var expandBabyMilk = document.getElementById("expandedBabyMilk");

        expandBabyMilk.src = imgs.src;

        expandBabyMilk.parentElement.style.display = "block";
    }

    function showBosniaIds(imgs) {
        $("#bosnia-ids").hide();

        var expandBosniaIds = document.getElementById("expandedBosniaIds");

        expandBosniaIds.src = imgs.src;

        expandBosniaIds.parentElement.style.display = "block";
    }

    function showCruiseMissileSkinTwo(imgs) {
        $("#cruise-missile-skin-two").hide();

        var expandCruiseMissileSkinTwo = document.getElementById("expandedCruiseMissileSkinTwo");

        expandCruiseMissileSkinTwo.src = imgs.src;

        expandCruiseMissileSkinTwo.parentElement.style.display = "block";
    }

    function showEveningStandard(imgs) {
        $("#evening-standard").hide();

        var expandEveningStandard = document.getElementById("expandedEveningStandard");

        expandEveningStandard.src = imgs.src;

        expandEveningStandard.parentElement.style.display = "block";
    }

    function showGitmoLock(imgs) {
        $("#gitmo-lock").hide();

        var expandGitmoLock = document.getElementById("expandedGitmoLock");

        expandGitmoLock.src = imgs.src;

        expandGitmoLock.parentElement.style.display = "block";
    }

    function showIraqiShelter(imgs) {
        $("#iraqi-shelter").hide();

        var expandIraqiShelter = document.getElementById("expandedIraqiShelter");

        expandIraqiShelter.src = imgs.src;

        expandIraqiShelter.parentElement.style.display = "block";
    }

    function showMaybeAirlinesStamps(imgs) {
        $("#maybe-airlines-stamps").hide();

        var expandMaybeAirlinesStamps = document.getElementById("expandedMaybeAirlinesStamps");

        expandMaybeAirlinesStamps.src = imgs.src;

        expandMaybeAirlinesStamps.parentElement.style.display = "block";
    }

    function showOlympicJerseys(imgs) {
        $("#olympic-jerseys").hide();

        var expandOlympicJerseys = document.getElementById("expandedOlympicJerseys");

        expandOlympicJerseys.src = imgs.src;

        expandOlympicJerseys.parentElement.style.display = "block";
    }

    function showSaddamInterviewTape(imgs) {
        $("#saddam-interview-tape").hide();

        var expandSaddamInterviewTape = document.getElementById("expandedSaddamInterviewTape");

        expandSaddamInterviewTape.src = imgs.src;

        expandSaddamInterviewTape.parentElement.style.display = "block";
    }

    function showSarajevoLibrary(imgs) {
        $("#sarajevo-library").hide();

        var expandSarajevoLibrary = document.getElementById("expandedSarajevoLibrary");

        expandSarajevoLibrary.src = imgs.src;

        expandSarajevoLibrary.parentElement.style.display = "block";
    }

    function showSimonBook(imgs) {
        $("#simon-book").hide();

        var expandSimonBook = document.getElementById("expandedSimonBook");

        expandSimonBook.src = imgs.src;

        expandSimonBook.parentElement.style.display = "block";
    }

    function showSniperSign(imgs) {
        $("#sniper-sign").hide();

        var expandSniperSign = document.getElementById("expandedSniperSign");

        expandSniperSign.src = imgs.src;

        expandSniperSign.parentElement.style.display = "block";
    }

    function showLeatherStrap(imgs) {
        $("#leather-strap").hide();

        var expandLeatherStrap = document.getElementById("expandedLeatherStrap");

        expandLeatherStrap.src = imgs.src;

        expandLeatherStrap.parentElement.style.display = "block";
    }

    function showUnLighter(imgs) {
        $("#un-lighter").hide();

        var expandUnLighter = document.getElementById("expandedUnLighter");

        expandUnLighter.src = imgs.src;

        expandUnLighter.parentElement.style.display = "block";
    }

    function showTankRound(imgs) {
        $("#tank-round").hide();

        var expandTankRound = document.getElementById("expandedTankRound");

        expandTankRound.src = imgs.src;

        expandTankRound.parentElement.style.display = "block";
    }

    function showPoliticalTeamPosters(imgs) {
        $("#political-team-posters").hide();

        var expandPoliticalTeamPosters = document.getElementById("expandedPoliticalTeamPosters");

        expandPoliticalTeamPosters.src = imgs.src;

        expandPoliticalTeamPosters.parentElement.style.display = "block";
    }

    function show1994MidtermNewspapers(imgs) {
        $("#1994-midterm-newspapers").hide();

        var expand1994MidtermNewspapers = document.getElementById("expanded1994MidtermNewspapers");

        expand1994MidtermNewspapers.src = imgs.src;

        expand1994MidtermNewspapers.parentElement.style.display = "block";
    }

    function showBullshipPoster(imgs) {
        $("#bullship-poster").hide();

        var expandBullshipPoster = document.getElementById("expandedBullshipPoster");

        expandBullshipPoster.src = imgs.src;

        expandBullshipPoster.parentElement.style.display = "block";
    }

    function showBushScarf(imgs) {
        $("#bush-scarf").hide();

        var expandBushScarf = document.getElementById("expandedBushScarf");

        expandBushScarf.src = imgs.src;

        expandBushScarf.parentElement.style.display = "block";
    }

    function showPunditSigns(imgs) {
        $("#pundit-signs").hide();

        var expandPunditSigns = document.getElementById("expandedPunditSigns");

        expandPunditSigns.src = imgs.src;

        expandPunditSigns.parentElement.style.display = "block";
    }

    function showObamaBidenSigns(imgs) {
        $("#obama-biden-signs").hide();

        var expandObamaBidenSigns = document.getElementById("expandedObamaBidenSigns");

        expandObamaBidenSigns.src = imgs.src;

        expandObamaBidenSigns.parentElement.style.display = "block";
    }

    function showObama08Signs(imgs) {
        $("#obama-08-signs").hide();

        var expandObama08Signs = document.getElementById("expandedObama08Signs");

        expandObama08Signs.src = imgs.src;

        expandObama08Signs.parentElement.style.display = "block";
    }

    function showWhiteHouseChristmasInvitation(imgs) {
        $("#white-house-christmas-invitation").hide();

        var expandWhiteHouseChristmasInvitation = document.getElementById("expandedWhiteHouseChristmasInvitation");

        expandWhiteHouseChristmasInvitation.src = imgs.src;

        expandWhiteHouseChristmasInvitation.parentElement.style.display = "block";
    }

    function showDeltaSnowGlobe(imgs) {
        $("#delta-snow-globe").hide();

        var expandDeltaSnowGlobe = document.getElementById("expandedDeltaSnowGlobe");

        expandDeltaSnowGlobe.src = imgs.src;

        expandDeltaSnowGlobe.parentElement.style.display = "block";
    }

    function showPresidentialTripStickers(imgs) {
        $("#presidential-trip-stickers").hide();

        var expandPresidentialTripStickers = document.getElementById("expandedPresidentialTripStickers");

        expandPresidentialTripStickers.src = imgs.src;

        expandDeltaPresidentialTripStickers.parentElement.style.display = "block";
    }